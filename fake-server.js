// yeah we don't need the server part of mu... this seems like the easiest way to disable it
const app = null;
const errorHandler = null;
exports.default = app;

exports.app = app;
exports.errorHandler = errorHandler;
